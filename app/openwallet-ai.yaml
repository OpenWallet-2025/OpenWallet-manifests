apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openwallet-ai
spec:
  project: openwallet
  destination:
    server: https://kubernetes.default.svc
  source:
    repoURL: https://github.com/OpenWallet-2025/openwallet-manifests
    path: services/ai
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
